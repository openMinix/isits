<div id="myform">
	<%= form_for(@client) do |f| %>
		<h1>New client</h1>
		
		<%= f.label :client_name %>
		<%= f.text_field :client_name, :class => 'signin-input' %>
	  
		<%= f.submit "Create client", :class => 'btn btn-primary signin-submit' %>
	  
	<% end %>

<%= link_to 'Back', clients_path %>
</div>