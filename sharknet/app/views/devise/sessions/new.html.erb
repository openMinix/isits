<div id="form-signin" class="form-sign-in">
	<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
			<h1>Sign in</h1>
			<%= f.label :email %>
			<%= f.email_field :email, :autofocus => true, :class => 'signin-input' %>

			<%= f.label :password %>
			<%= f.password_field :password, :class => 'signin-input' %>

			<% if devise_mapping.rememberable? -%>
				<%= f.check_box :remember_me, :class => 'signin-input' %> <%= f.label :remember_me %>
			<% end -%>

			<%= f.submit "Sign in", :class => 'signin-submit' %>
	<% end %>
	<%= render "devise/shared/links" %>
</div> 


